import AdyenEncryptionHelper from '../AdyenEncryptionHelper';

describe('Adyen encryption helper:', () => {

  it('should download adyen encryption script if it is not exist in global window', () => {
    expect(null).toBeNull();
  });

});
